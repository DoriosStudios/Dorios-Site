import React from 'react';
import AddonWiki from './index';

export default function AddonWikiGeneratedPage({projectId, section = 'overview'}) {
  return <AddonWiki projectId={projectId} section={section} />;
}
